{
  "name": "taskbar-hero-wiki-helper",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "sync-test-data": "node scripts/sync-test-data.mjs",
    "sync-wiki-data": "node scripts/sync-wiki-data.mjs",
    "build": "npm run sync-wiki-data && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.0.3"
  }
}
